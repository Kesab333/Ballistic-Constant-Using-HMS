# Ballistic-Constant-Using-HMS


Bug Fixes

1. Whenever Refresh- Intially the lab scene loads RB and HMS controls
2. Wires are going inside the casing of apparatus - It should connect to terminals without going inside the case
3. Remove Terminals drag - It Creates a very big solid cylinder across the terminals
4. When Im dragging apparatus on table --> there is no apparatus available in their seperate sections ---Fix it
5. In lab Controls&#x20;

&#x20;      Remove Auto lever up-down

add Raise and Drop so now the experiment can perform like real bcz in reality we are droping at some resistance and noting the value&#x20;

In mannual connect -- Auto drop bcz of gravity in not working which i have implemented in HMS Section controls

Tapping Switch ---> Not Updating its status of ON/OFF that makes confusion either its ON or OFF

6. For Wrong Connection --> We are not getting notification&#x20;

wrong connection doesnt mean predefined connection, wrong connection means like i have connected both terminals of RB/HMS or any apparatus with itself only or many wanted connected like connected both terminal of RB with HMS only these type of connections define as wrong connection--- Give Reason and solution of wrong connections

7. For mannual connect --> there should also show some notification that terminal is selected or not otherwise how we know is it selected or not